- name: Run Backup Script
  win_shell: |
    .\backup.ps1 -adfspasswd "{{ adfs_backup_password }}"
  args:
    chdir: "{{ scripts_path }}\\scripts"
    no_profile: true

- name: Run Firewall Script
  win_shell: |
    .\firewall.ps1 -LockoutPrevention $false -ExtraRules httpio,httpsio,{{ scored_services }} -installWFC $false -addIpsFromFile "datadog_ips.txt",443 -siemIP "{{ siem_IP }}" -siemName "{{ siem_name }}" -passmgrIP "{{ password_manager_ip }}" -stabvestIP "{{ stabvest_ip }}"
  args:
    chdir: "{{ scripts_path }}\\scripts"
    no_profile: true
